# מסמך אפיון — TabToTable AI

**גרסה נוכחית:** 0.7.0 (MVP) · **סטטוס:** בפיתוח, מוכן לבדיקה עצמית · **עדכון אחרון:** 2026-09-15

---

## 1. מה זה בכלל

TabToTable AI הוא תוסף כרום שלוקח את כל הלשוניות הפתוחות בחלון דפדפן אחד, קורא את התוכן שלהן, ובלחיצת כפתור אחת בונה מהן טבלת השוואה מסודרת אחת — עם עמודות, שורות, וקישור חזרה לכל מקור.

זה לא כלי גדול או מסובך. זה כלי אחד שפותר בעיה מאוד ספציפית וחוזרת: **הפיזור של מידע בין עשרות לשוניות פתוחות כשעושים מחקר**.

---

## 2. הבעיה

כשעושים מחקר מתחרים, סקר שוק, או השוואת כלים/ספקים/מוצרים — הדפוס תמיד זהה:

1. פותחים 10, 20, לפעמים 40 לשוניות — כל אחת אתר של מתחרה, כלי, או מוצר.
2. עוברים לשונית-לשונית, ומעתיקים ידנית: שם, מחיר, פיצ'רים, יתרונות.
3. מדביקים הכל לגיליון אקסל או מסמך Word, ומנסים לארגן את זה בעצמם.
4. זה לוקח שעה-שעתיים על עבודה שהמוח האנושי לא טוב בה (העתקה חוזרת, ריכוז נתונים, שמירה על עקביות בין שורות).

זו לא בעיה של "אין מספיק כלים" — יש Notion, Excel, Google Sheets. הבעיה היא **המעבר הידני בין המקור (הלשונית) ליעד (הטבלה)**. אין כלי שסוגר את הפער הזה אוטומטית.

---

## 3. הפתרון — איך זה עובד בפועל

### זרימת המשתמש (User Flow)
1. המשתמש פותח את כל הלשוניות הרלוונטיות למחקר בחלון דפדפן אחד.
2. לוחץ על אייקון התוסף.
3. (אופציונלי) כותב שורת הקשר קצרה — למשל "השוואת תמחור בין כלי CRM" — כדי לכוון את ה-AI מה חשוב להשוות.
4. לוחץ "בנה טבלה מהלשוניות הפתוחות".
5. תוך כמה שניות נפתחת לשונית חדשה עם טבלה מוכנה, הניתנת לייצוא ל-CSV.

### מה קורה מאחורי הקלעים (טכני)
1. **סריקה** — `background.js` (Service Worker של התוסף) שולף את רשימת כל הלשוניות הפתוחות בחלון הנוכחי (`chrome.tabs.query`), ומסנן רק לשוניות עם כתובת `http/https` תקינה (לא `chrome://`, לא לשוניות מוגנות).
2. **חילוץ תוכן** — לכל לשונית מוזרק סקריפט קצר (`chrome.scripting.executeScript`) שקורא מתוך הדף: כותרת, תיאור מטא, כותרות H1/H2 עיקריות, וטקסט גוף הדף (עד 4,000 תווים, כדי לשמור על עלות טוקנים סבירה).
3. **ניתוח ובניית טבלה** — כל התוכן שנאסף נשלח בבקשה אחת ל-Claude API (מודל `claude-sonnet-5`), עם הנחיה ברורה: לזהות מה משותף לדפים, לבחור עמודות רלוונטיות, ולהחזיר טבלה במבנה JSON קבוע (`columns` + `rows`).
4. **הצגה** — הטבלה נשמרת מקומית (`chrome.storage.local`) ומוצגת בלשונית חדשה (`results.html`) עם אפשרות ייצוא ל-CSV.

### מה שהמערכת *לא* עושה (בכוונה, בשלב הזה)
- לא שולחת מידע לשום שרת מלבד Anthropic API ישירות מהדפדפן של המשתמש.
- לא שומרת תוכן דפים בענן — הכל מקומי בדפדפן.
- לא מבצעת גלישה אוטומטית או "קליק" על דפים — רק קוראת תוכן של לשוניות שהמשתמש כבר פתח בעצמו.

---

## 4. למה כדאי להשתמש בזה (Value Proposition)

| למי | למה זה שווה |
|---|---|
| אנשי שיווק/מכירות | מחקר מתחרים מהיר לפני פגישה, בלי לבזבז שעה על העתק-הדבק |
| מייסדים/מנהלי מוצר | השוואת כלים/ספקים (SaaS, פלטפורמות, ספקי שירות) לפני החלטת רכש |
| אנליסטים/חוקרי שוק | ריכוז מידע ממספר מקורות למבנה אחיד שניתן לייצא ולעבד הלאה |
| כל מי שסוגר טאבים בלי לתעד מה היה בהם | הטבלה היא "זיכרון" מסודר של המחקר, לא רק תוצאה חד-פעמית |

**הערך המרכזי:** הפיכת כאוס (עשרות לשוניות, בלי מבנה) לפלט עבודה מיידי (טבלה, ברת-ייצוא, ברת-שיתוף) — בזמן שלוקח לעשות קפה, לא בשעה.

---

## 5. תכונות בגרסה הנוכחית (v0.7.0)

- סריקת עד 40 לשוניות בחלון נוכחי בלחיצה אחת.
- **בחירת לשוניות ידנית** — לפני הסריקה, המשתמש יכול להסיר לשוניות לא-רלוונטיות מתוך רשימה מתקפלת בפופאפ (checkbox לכל לשונית, favicon+כותרת), במקום לשלוח תמיד את כל הלשוניות הפתוחות. ברירת המחדל: הכול מסומן (זהה להתנהגות הישנה).
- הבנת הקשר אוטומטית, או לפי שורת הקשר שהמשתמש מזין.
- טבלה דינמית — העמודות נבחרות על ידי ה-AI לפי תוכן הדפים בפועל, לא template קבוע.
- **עדכון/Refine לאחר בנייה** — בדף התוצאות אפשר לבקש מ-Claude לעדכן את הטבלה הקיימת (הוספת/הסרת עמודה, סינון שורות, מיון, ניתוח נוסף) בלי לחזור לסרוק את הלשוניות; כל עדכון נשמר כרשומת היסטוריה נפרדת.
- ייצוא ל-CSV (תואם עברית/RTL, נפתח נכון באקסל) ול-Markdown.
- קישורים חיים לכל מקור בתוך הטבלה.
- מכסת שימוש חינמית: 150 לשוניות סריקה בחודש (נאכפת בצד הלקוח בשלב הזה — ראו מגבלות).
- שמירת מפתח Anthropic API מקומית בלבד בדפדפן.
- חילוץ תוכן מהלשוניות מקבילי (לא סדרתי), timeout+ניסיון חוזר יחיד על כשל זמני בקריאות ל-Anthropic API.

## 6. מגבלות ידועות (v0.1)

- **אכיפת מכסה בצד לקוח בלבד** — לא עמידה מול משתמש שרוצה לעקוף אותה (איפוס local storage). זו התנהגות UX זמנית, לא אבטחה אמיתית.
- **אין תוכנית תשלום אמיתית** — אין Stripe, אין billing. "השדרוג לתוכנית בתשלום" הוא כרגע רק טקסט placeholder.
- **אין ייצוא אוטומטי ל-Monday.com** — מתוכנן, לא קיים.
- **תלות במפתח API אישי** — כל משתמש צריך מפתח Anthropic משלו; אין שכבת חיוב מרכזית של החברה.
- **טקסט בלבד** — לא קורא תמונות, גרפים, או PDF מוטמע בדף.
- **דפים דינמיים כבדים (SPA שטוען תוכן ב-JS לאט)** — אם התוכן עוד לא נטען כשהתוסף סורק, ייתכן שיחסר מידע.

## 7. מודל עסקי (מתוכנן)

- **חינם** — עד 150 לשוניות סריקה בחודש. מתאים למחקרים קצרים/מזדמנים.
- **בתשלום (עתידי)** — מכסה מורחבת/ללא הגבלה, ייצוא אוטומטי בזמן אמת ל-Monday.com ולמערכות ניהול פרויקטים נוספות, שמירת היסטוריית מחקרים.

## 8. מפת דרכים (Roadmap)

| שלב | מה נדרש | תלות |
|---|---|---|
| v0.2 | שיפורי אמינות (retry, טיפול בדפים כבדים), שמירת היסטוריית טבלאות בתוך התוסף | אין |
| v0.3 | Backend קליל (Cloudflare Worker) לאכיפת מכסה אמיתית + מפתח API מרכזי (המשתמש לא צריך מפתח משלו) | החלטה על ספק Backend |
| v0.4 | חיבור OAuth ל-Monday.com + ייצוא אוטומטי | v0.3 (צריך backend לניהול טוקנים) |
| v0.5 | פרסום בחנות Chrome Web Store | מדיניות פרטיות רשמית, בדיקות אבטחה, תשלום דמי מפתח |

## 9. מדדי הצלחה (להערכה עתידית)

- זמן ממוצע מלחיצה על "בנה טבלה" ועד טבלה מוכנה (יעד: מתחת ל-15 שניות ל-10 לשוניות).
- אחוז לשוניות שנסרקו בהצלחה מתוך סה"כ לשוניות (יעד: מעל 90% על דפים ציבוריים רגילים).
- שימור משתמשים חודשי (Retention) לאחר שדרוג ל-backend אמיתי.

## 10. מבנה תיקייה (v0.7.0 — תואם `_AUDIT/STANDARDS.md` §1)

```
TabToTable-AI/
├── manifest.json              ← MV3, default_locale=en, כל הנתיבים יחסית לשורש
├── TabToTable-AI-v0.7.0.zip   ← ZIP סופי אחד, נבנה על ידי build/build.ps1
├── CHANGELOG.md, SPEC.md, README.md (he+en), PRIVACY.md, EULA.md, DELETIONS.md
├── _locales/en/, he/messages.json   ← מקור אמת יחיד לכל מחרוזות UI (Chrome i18n סטנדרטי)
├── src/
│   ├── background/background.js
│   ├── popup/popup.html, popup.js
│   ├── options/options.html, options.js
│   ├── results/results.html, results.js
│   └── shared/brand.css, i18n.js
├── assets/icons/               ← 16/32/48/128px מלא, תואם §15.4
├── build/build.ps1             ← build/validation בפקודה אחת (§9), לא נראה בשורש
├── site/landing-page.html
└── store/LISTING.md            ← תיאור לחנות he+en (טרם צילומי מסך)
```

**שפת ממשק (he/en):** מיושמת דרך `_locales/*/messages.json` האמיתיים של Chrome — לא רק לשדות `manifest.json` (`__MSG_appName__`/`__MSG_appDesc__`), אלא גם דרך `src/shared/i18n.js` שטוען את אותם קבצי JSON בזמן ריצה ומאפשר מתג שפה חי בתוך התוסף (מה ש-`chrome.i18n.getMessage()` הטהור לא מאפשר — הוא נעול לשפת הדפדפן). **ברירת מחדל: אנגלית**, בהתאם לכלל הכללי (`_AUDIT/STANDARDS.md` §4) — לא זיהוי אוטומטי מה-OS.

**הרשאת `<all_urls>` — הצדקה מתועדת:** התוסף צריך לקרוא תוכן מכל הלשוניות הפתוחות בחלון (לא רק הלשונית הפעילה), כדי לבנות טבלת השוואה מכולן יחד. `activeTab` בלבד מספיק רק ללשונית הפעילה בזמן הלחיצה — לא לכל שאר הלשוניות הפתוחות. לכן `host_permissions: ["<all_urls>"]` הוא צורך פונקציונלי אמיתי, לא over-permissioning רשלני (כבר תועד ב-`_AUDIT/STANDARDS.md` §8 כחריג מוכר).

## 11. מגבלות ידועות לגבי תקן STANDARDS.md (v0.7.0)

**נסגר בסבב הזה (v0.7.0):** תכונת מוצר לפי מחקר מתחרים (בחירת לשוניות ידנית + refine לאחר בנייה — ראו `CHANGELOG.md`), חילוץ תוכן מקבילי במקום סדרתי, timeout+retry יחיד על קריאות Anthropic API, ליטוש UI (אנימציית כניסה לטבלה, משוב `:active` לכפתורים) — כל זאת בלי לשנות את פלטת הצבעים הקיימת (ענבר/זהב, לא כחול/נייבי — ראו הערה ב-`CHANGELOG.md` v0.7.0).

**נסגר בסבב קודם (v0.6.0):** אייקון 32px (נוצר ונוסף ל-manifest), עברית קשיחה שנשארה ב-`results.html` גם בממשק אנגלי (h1/כפתורים/placeholder/title הומרו ל-`data-i18n`), `text-align: right` קשיח בטבלת התוצאות (הומר ל-`text-align: start` הלוגי), padding פיזי לא-סימטרי ב-`.tab-chip`, וסקריפט `build/build.ps1` נכתב **והורץ בפועל** (לא רק נכתב על הנייר).

**עדיין פתוח:**
- **צילומי מסך לחנות** — לא ניתן לצלם ריצה חיה של Chrome בסביבה הזו; `store/LISTING.md` מוכן טקסטואלית בלבד.
- **בדיקת ניגודיות WCAG AA פורמלית** — לא הורצה בכלי אוטומטי; טוקני הצבע בנויים על ניגודיות גבוהה מכוונת (`--ink`/`--ground` וכו') אך לא נמדדה בפועל עם GUI חי.
- **בדיקת DPI/רספונסיביות בפועל** — לא רלוונטי לפופאפ (גודל קבוע לפי מוסכמת Chrome), אך `options_page`/`results.html` לא נבדקו ב-GUI חי בחלון דפדפן צר (אין סביבת דפדפן גרפית זמינה כאן; רק ביקורת קוד סטטית).
- **`CHANGELOG.md`** — הערך החדש (v0.6.0) הוא דו-לשוני מלא; רשומות ישנות יותר (v0.1.0–v0.5.0) נשארות עברית-בלבד כדי לא להכפיל תוכן היסטורי גדול — `README.md`/`PRIVACY.md`/`EULA.md` כן דו-לשוניים מלאים לגמרי.
- **Badge על אייקון הפעולה** (§13.1) — לא מיושם; לא קריטי לתוסף שפועל בלחיצה בודדת ולא במעקב מתמשך.
- **`site/landing-page.html`** — אין `lang`/`dir` דינמיים ואין מתג שפה (§17.0); לא נגעו בסבב הזה כי לפי הנחיית המשתמש דף הנחיתה/האתר השיווקי מתעדכן בנפרד. אם `site/` הזה כן נחשב חלק מהתוסף (לא ה-hub המרכזי) — זה עדיין פער פתוח מול §17.
- **בדיקה חזותית עם GUI חי** — כל האימות בסבב הזה נעשה בביקורת קוד סטטית + `node --check`/JSON validation + הרצת `build/build.ps1` בפועל; לא הורצה טעינה ממשית של התוסף ב-`chrome://extensions` עם GUI חי (אין סביבת Windows גרפית זמינה כאן).
- **v0.7.0 — הרחבת אותה מגבלה:** ה-tab picker (checkboxes בפופאפ) ותכונת ה-refine (דף התוצאות) לא נטענו/נלחצו בפועל בדפדפן חי — נבדקו רק דרך `node --check`, קריאת קוד, ווידוא שמפתחות ה-i18n קיימים בשתי השפות. לפני שילוח: לטעון את התוסף ב-`chrome://extensions` (מצב מפתח) ולוודא בפועל שה-checkboxes מתעדכנים נכון, שסריקה עם תת-קבוצה של לשוניות שולחת רק אותן, ושכפתור ה-refine מציג/מעדכן טבלה אמיתית.
